# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.25.0

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.0

# Settings
pydantic-settings>=2.0.0

# Task Queue (PostgreSQL-backed)
procrastinate>=3.0.0
psycopg[binary]>=3.1.0

# HTTP Client
httpx>=0.26.0

# RSS Parsing
feedparser>=6.0.0

# LLM Client (OpenAI compatible)
openai>=1.0.0

# Video Download
yt-dlp>=2024.1.0

# HTML Parsing
beautifulsoup4>=4.12.0
lxml>=5.0.0
trafilatura>=2.0.0

# Web Crawling (primary content extraction, connects to Browserless via CDP)
crawl4ai>=0.5.0

# Finance Data
akshare>=1.18.52

# Ebook Parsing
ebooklib>=0.18

# Security
cryptography>=42.0.0

# MCP Server
fastmcp>=2.0.0
# mcp_server.py 使用 mcp v1 内置 FastMCP（mcp.server.fastmcp）；mcp 2.x 已改名 MCPServer，
# 迁移前锁定 v1（旧镜像验证过的组合: mcp 1.29.1 + fastmcp 3.4.7）
mcp>=1.9,<2

# Utilities
python-multipart>=0.0.6
python-dotenv>=1.0.0
