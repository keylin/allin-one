# 10 立项

> 回答：这个系统是什么、给谁用、用在什么场景、关键概念指什么。
> 核实日期：2026-09-24

## 1. 项目背景

**定位**：allin-one 是 the-one（数字大脑）的感知与记录层——部署在家庭服务器的个人内容与数据服务。目标是可独立离线运行；现状采集、TMDb 补全、Google Books 补全仍出网。它存"记录"（读过什么、看过什么、记了什么、有什么），the-one 存"判断"（画像、研判、预测、复盘）。

**演进**：

| 时间 | 节点 |
|---|---|
| 2026-02 | 从 RSS 聚合起步（2-11 首个提交；B 站 2-12 已作为采集器接入）；加 EPUB/MOBI 在线阅读器（2-21），四天后拆除，改为电子书元数据与标注只读展示（2-25）；Apple Books 同步（2-25）；同步架构改为"外部脚本推送"，接入微信读书、Kindle、Safari/Chrome 书签，B 站改为同步器（2-26） |
| 2026-03 | MCP server 新建（3-05，供 AI CLI 查询信息流）；扩展数据源管理与收藏统计（3-10）；切换官方 SDK（3-18） |
| 2026-08-30 | 从旧运行环境迁到家庭服务器 home-server（192.168.1.103；原快照 `allinone_20260228_042448.sql` 留在 Mac）。只恢复了 35 条数据源定义，历史内容、媒体、阅读进度、书摘未恢复 |
| 2026-09-04 | MCP 金融工具切换蚂蚁 financial-data 主源 |
| 2026-09-16 | 影视资料库上线（Emby 同步 + 手工添加 + 观看标记 + MCP 三工具） |
| 2026-09-20 | 架构审计：确立"两条主线"、`content_items.kind`、源类型注册表单点、保留策略单点 |
| 2026-09-23 | 定位为 the-one 的一部分，仓库移入 `the-one/allin-one/`（git 独立）；确定扩成全面个人内容系统；数据模型与存储模型定稿（`design_data_model.md`）；备份脚本落地（timer 停用，手动跑） |

**部署**：源码 `/root/workspace/the-one/allin-one`（git，远程 github.com/keylin/allin-one），生产目录 `/opt/allin-one`（rsync 镜像），Docker Compose 七个容器，站点绑定 `192.168.1.103:8000`，公网入口 `https://one.nillin.cc`（ECS 反代，见 the-one `project/家庭网络/02-运维手册.md`）；MCP 绑定 `192.168.1.103:8001`，无应用层认证，不配置公网反代。

## 2. 应用场景

**实际使用行为**（2026-09 观察）：

- 手机：刷信息流、看影视库、标记看过/想看。
- 桌面终端（the-one 会话）：`/digest` 拉信息流做研判；`/mentor`、`/brainstorm` 按关键词取内容；`/trader` 只经 MCP 取美股 ETF 与加密货币行情；影视库经 MCP `list_films` / `mark_films` 读写（用作推荐与批量标记，属使用习惯，无日志可证）。宏观指标不走本系统：the-one 的 skill 文档标注 `get_macro_indicator` 数据停更于 2025-09，宏观走 financial-data-query。
- Emby 看片后 30–40 分钟内自动同步观看事实到影视库（每 10 分钟检查，间隔满 30 分钟才跑）。
- 电子书标注数据为空；备忘有提交入口（数据源页对 `user.note` 源「提交内容」→ `POST /api/content/submit`）但无数据；日记无入口。

**数字痕迹覆盖**：

| 类别 | 痕迹 | 今日覆盖 |
|---|---|---|
| 摄入 | 文章、播客 | 信息流：57 个 RSS 源（43 启用）、1 个网页抓取源（停用）、6 个播客源；文章 10,301 条、播客 173 条（全库 11,651 条含影视，2026-09-24） |
| 摄入 | 影视 | 影视库：1,177 部，Emby 同步 + 手工 |
| 摄入 | 书与标注 | 图书馆：功能在，数据为空（迁移未恢复） |
| 摄入 | B 站视频 | 同步源存在并已关联 cookie 凭证（status=active，有效性未验），源停用 |
| 摄入 | 音乐 | 无 |
| 产出 | 备忘、日记、AI 会话 | 备忘：有提交入口，无数据；日记：无入口；AI 会话：无入口（单条内容的对话 `POST /api/content/{id}/chat` 存 `chat_history`，无数据）；`user_note` 字段存在但无数据 |
| 交流 | 消息、邮件、日历 | 无 |
| 财务 | 行情 | 经 MCP 直取（蚂蚁主源，AkShare / 雪球降级），不落库；`finance_data_points` 表存在但 0 行，库中无 `api.akshare` 数据源；个人流水无 |
| 身体 / 时空 / 环境 | 体检、位置、家居 | 无 |
| 影像 | 照片 | 无 |
| 判断 | 画像、研判 | 不在本系统，在 the-one |

**the-one 侧的消费方式**：`scripts/intel-query.py`（REST，`X-API-Key`）按「情报」过滤器圈定的源拉内容，以水位线做增量；MCP server（192.168.1.103:8001）提供 20 个工具，见 50-用例。

## 3. 概念辨析

系统内正在使用的概念，含义以代码与文档为准：

| 概念 | 含义 |
|---|---|
| **信息流 / 资料库** | 两条主线。信息流：采集来的时效内容（RSS、播客、抓取），进流水线，按保留期清理。资料库：影视、电子书、书签、笔记、上传文件、同步来的视频，不进流水线，永不自动清理。 |
| **kind** | `content_items.kind`，内容的领域身份，写入时确定。现值：article、audio、video、book、film、bookmark、note、file。 |
| **数据源 / 订阅源** | 数据源（`source_configs`）是"从哪来"的配置，统一术语；订阅源专指 RSS feed。 |
| **源类型** | `source_types.py` 注册表的一条：执行器（collector / syncer / push / none）、默认 kind、可否调度、是否需凭证、领域、自动同步间隔、是否已实现。 |
| **采集 / 同步 / 推送** | 采集：Collector 增量追加，可调度。同步：内置 Syncer 全量对账（Emby、微信读书、B 站）。推送：外部脚本或 Fountain 客户端 POST 到 `/api/{ebook,video,bookmark}/sync`。 |
| **流水线** | 由模板显式定义的步骤序列（抽取、抓全文、翻译、媒体本地化、转写、LLM 分析、推送），经 `source.pipeline_template_id` 绑定；无模板则内容直接 ready。 |
| **收藏 / 已打开 / 已读** | 收藏：`is_favorited`，保留策略豁免，触发媒体本地化。已打开：`opened_at`，仅详情接口写。已读：`view_count` / `last_viewed_at`，含滚动与批量标记，与 app 端共享，不作研判依据。 |
| **情报过滤器** | `system_settings.content.filters` 里名为「情报」的过滤器，圈定 the-one 研判用的源；过滤器之间允许重叠。 |
| **记录 / 判断** | 分界规则：会被反复改写的（画像、话题、研判手册、预测、复盘）归 the-one；只追加不重写的（一条内容、一次观看、一条标注）归 allin-one。the-one 引用本系统对象写 `content:<id>`，不复制正文。 |
| **自有副本** | 落地到本系统的资源（海报、插图、封面）都是自有副本，不视为可从上游重拉的缓存，都在备份范围内。 |
| **反射 / 认知** | 反射：单条内容、不依赖 doctrine 的处理（抽取、摘要、标签、去重），在本系统流水线。认知：跨条目、依赖研判手册与记忆的判断，在 the-one skill。 |
